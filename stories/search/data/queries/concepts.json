{
    "bool": {
        "should": [
            {
                "term": {
                    "variants": "{{query}}"
                }
            },
            {
                "term": {
                    "name": "{{query}}"
                }
            },
            {
                "term": {
                    "mesh_preferred_name": "{{query}}"
                }
            },
            {
                "term": {
                    "lcsh_preferred_name": "{{query}}"
                }
            },
            {
                "term": {
                    "wikidata_preferred_name": "{{query}}"
                }
            },
            {
                "term": {
                    "wikidata_id": "{{query}}"
                }
            },
            {
                "term": {
                    "lcsh_id": "{{query}}"
                }
            },
            {
                "term": {
                    "mesh_id": "{{query}}"
                }
            }
        ]
    }
}
